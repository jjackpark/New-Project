# T008 - 상태값(enum) 공통 뱃지/드롭다운 컴포넌트

- **phase**: 0-foundation
- **priority**: critical
- **status**: done
- **estimated_minutes**: 30
- **dependencies**: T005
- **tags**: ui, component, enum

## 설명

SPEC 5.5 상태값 전체 목록을 기반으로 공용 색상 뱃지 및 드롭다운 선택 컴포넌트를 구현한다 (자유텍스트 금지).

## 참고

- 원본 스펙: SPEC.md
- 관련 작업 목록: backlog.json (id: T008)

## 작업 노트

_(진행하면서 결정사항, 이슈, 리뷰 코멘트를 이 아래에 기록)_

- 2026-09-17: `web/app-core.js`에 `App.ENUMS`로 SPEC.md §5.5의 10개 enum 필드×허용값을 전부 색상(green/gray/red/amber/blue)에 매핑하고, `App.ui.badge(field, value)`(뱃지 렌더)와 `App.ui.enumOptions(field, selected)`(`<option>` 목록, 자유 텍스트 입력 없음)로 공용 컴포넌트화했다.
  - 13개 탭 전체(설비/마스터플랜/Qual/런평가/이슈/알람/일보/프로젝트/계정)의 상태 표시·입력 폼이 전부 이 두 함수만 사용 — 상태값을 자유 텍스트로 받는 곳이 없다.
  - 브라우저에서 각 상태값 뱃지 색상과 드롭다운 옵션이 SPEC 표와 일치하는지 확인했다. adversarial-reviewer는 사용자 지시로 사용하지 않았고 lint/build + 수동 브라우저 검증으로 대체했다.
