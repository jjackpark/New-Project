# T010 - users 테이블 및 API

- **phase**: 1-core-data
- **priority**: 1
- **status**: done
- **estimated_minutes**: 30
- **dependencies**: T003
- **tags**: api, users

## 설명

아이디/비밀번호/이름/권한(admin/worker) 필드를 갖는 users CRUD API를 구현한다.

## 참고

- 원본 스펙: SPEC.md
- 관련 작업 목록: backlog.json (id: T010)

## 작업 노트

_(진행하면서 결정사항, 이슈, 리뷰 코멘트를 이 아래에 기록)_

- 2026-09-17: 사용자 지시로 실제 서버 API 대신 localStorage 기반 App.store(web/app-store.js)를 데이터 계층으로 채택, 이 수준을 API로 인정하기로 했다. users CRUD는 App.store.list/get/insert/update("users", ...)로 동작하고 password 필드까지 포함(web/data-seed.js). 나중에 실제 서버가 생기면 App.store 내부만 fetch로 교체.
