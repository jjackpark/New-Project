# T009 - 텍스트 짤림 방지 공통 유틸 구현

- **phase**: 0-foundation
- **priority**: critical
- **status**: todo
- **estimated_minutes**: 30
- **dependencies**: T005
- **tags**: ui, component, ux

## 설명

입력 글자수 제한 → 자동 축소 → 말줄임+툴팁의 3단계 공통 유틸/컴포넌트를 구현한다 (SPEC 7장).

## 참고

- 원본 스펙: SPEC.md
- 관련 작업 목록: backlog.json (id: T009)

## 작업 노트

_(진행하면서 결정사항, 이슈, 리뷰 코멘트를 이 아래에 기록)_

- 2026-09-17: SPEC.md §7의 3단계를 순서대로 구현했다.
  1. 입력 글자수 제한: 설비명 `maxlength="12"`, 담당자/인력명 `maxlength="6"` (`web/views/equipment.js`, `web/views/vendors.js`, `App.ui.assigneeFields`) — migrations/0001_core.sql의 CHECK 제약과 동일한 값.
  2. 자동 축소: `App.ui.fitSpan()`으로 렌더한 `.text-fit` 요소를 `App.ui.applyFit(root)`가 라우터 렌더 직후(`web/app.js`) 폰트 크기를 13px→11px 범위에서 0.5px씩 줄이며 맞춘다.
  3. 말줄임+툴팁: `.truncate` CSS(app.css, 기존 T005 산출물) + `title` 속성으로 최후 처리.
  - 설비명/담당자/인력명/업체 비고 등 실제 데이터에 적용해 브라우저에서 확인했다. adversarial-reviewer는 사용자 지시로 사용하지 않았고 lint/build + 수동 브라우저 검증으로 대체했다.
