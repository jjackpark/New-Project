# T049 - photos 테이블 및 파일 저장 API

- **phase**: 4-extension
- **priority**: 4
- **status**: in_progress
- **estimated_minutes**: 30
- **dependencies**: T012
- **tags**: api, photos, file-storage

## 설명

설비/설명/파일경로 필드와 서버 PC 폴더 저장, DB엔 경로만 기록하는 업로드 API를 구현한다.

## 참고

- 원본 스펙: SPEC.md
- 관련 작업 목록: backlog.json (id: T049)

## 작업 노트

_(진행하면서 결정사항, 이슈, 리뷰 코멘트를 이 아래에 기록)_

- 2026-09-17: 메타데이터(설비/설명/file_path/등록자/시각) CRUD는 App.store("photos")로 이미 동작하지만(web/views/photos.js), 이 태스크의 핵심인 **실제 파일 바이너리를 서버 PC 폴더에 저장하는 부분은 브라우저 localStorage로 대체할 수 없어 미완료로 남긴다** — 다른 "OOO 테이블 및 API" 태스크와 달리 진짜 파일시스템 I/O가 필요하기 때문. 지금은 `<input type="file">`에서 파일명만 읽어 file_path 텍스트로 기록한다. 실제 서버(src/index.js)에 업로드 엔드포인트 + 파일 저장 로직이 붙을 때 이 태스크를 마무리한다.
