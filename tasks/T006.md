# T006 - 프로젝트 스코프 전환 전역 상태 관리

- **phase**: 0-foundation
- **priority**: critical
- **status**: todo
- **estimated_minutes**: 30
- **dependencies**: T005
- **tags**: state, project-scope

## 설명

프로젝트가 전역 최상위 스코프이므로, 현재 선택된 프로젝트를 앱 전역에서 참조/전환하는 상태 관리를 구현한다.

## 참고

- 원본 스펙: SPEC.md
- 관련 작업 목록: backlog.json (id: T006)

## 작업 노트

_(진행하면서 결정사항, 이슈, 리뷰 코멘트를 이 아래에 기록)_

- 2026-09-17: `web/app-store.js`의 `App.state.getCurrentProjectId/setCurrentProjectId`로 구현. localStorage(`sm_current_project`) 기준이며, 저장된 값이 없거나 삭제된 프로젝트를 가리키면 첫 번째 프로젝트로 폴백한다.
  - 상단바(`web/app.js`의 `renderTopbarProject`)에 프로젝트 선택 드롭다운을 두어 전역 전환 UI 제공, 모든 뷰(설비/업체/마스터플랜/Qual/이슈/알람/일보/캘린더/사진/검색)가 이 값으로 필터링한다.
  - `web/views/projects.js`의 "선택" 버튼도 같은 API를 호출 — 프로젝트 탭과 상단바 두 진입점이 상태를 공유.
  - 13개 탭 전체를 브라우저에서 프로젝트 전환하며 직접 확인했다(P1라인 셋업 ↔ P2증설). adversarial-reviewer는 사용자 지시로 이번 세션에서 사용하지 않았고, lint/build 통과 + 수동 브라우저 검증으로 대체했다.
