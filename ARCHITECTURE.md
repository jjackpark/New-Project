# ARCHITECTURE.md

T001(기술 스택 및 아키텍처 결정)의 산출물이다. SPEC.md §3(제약 조건)을 만족하는 런타임·프레임워크·DB·배포 방식을 결정한다. 이 결정은 opus 모델의 적대적 리뷰를 거쳐 초안(v1)에서 발견된 5건의 치명적 결함을 반영해 수정한 v2다 — 반영/보류 내역은 문서 끝의 "리뷰 반영 로그"를 본다.

## 1. 런타임 / 버전 고정

- **Node.js 24.x LTS로 고정한다.** 개발 머신(v24.18.0)에서 `node:sqlite`가 플래그 없이 정상 동작함을 직접 확인했다. Node 22.x는 `node:sqlite`가 `--experimental-sqlite` 플래그를 요구하고 API도 더 적어(예: `timeout` 옵션 없음) 채택하지 않는다.
- 빌드에 쓰는 Node 버전과 배포용 SEA를 만드는 Node 버전은 **반드시 같아야 한다.** 저장소 루트에 `.nvmrc`(`24`)를 두고, T002에서 CI/로컬 모두 이 파일을 참조하도록 스캐폴딩한다.

## 2. 패키징: Node SEA + 단일 CJS 번들

- 배포 형태는 Node의 Single Executable Application(SEA, `node --experimental-sea-config`)으로 만든 exe 하나. Electron은 배제한다(다중 사용자가 각자 브라우저로 접속하는 서버-클라이언트 구조와 "경량" 요구에 맞지 않음 — 통상 100MB+, 1인 1앱 가정).
- SEA의 진입 스크립트는 **내장 모듈 외의 `require()`를 지원하지 않으므로**, 서버 소스를 여러 파일로 나눠 작성하되(§5 300줄 제한 준수) **빌드 시 esbuild로 단일 CJS 파일로 번들**한 뒤 SEA 처리한다.
- **원칙 수정**: "외부 의존성 0"은 *런타임(dependencies)*에만 적용한다. 번들러(esbuild)·린터 등은 **devDependencies로 허용**한다. exe 자체에는 포함되지 않으므로 배포 조건과 충돌하지 않는다.
- T002에서 `package.json`에 `"lint"`(eslint 또는 oxlint)와 `"build"`(esbuild 번들 + SEA 생성) 스크립트를 추가한다 — RULES.md §5의 lint/build 훅이 이 시점부터 실제로 동작하게 된다.

## 3. 정적 자산 서빙

- `__dirname` 기준 `fs.readFile`로 정적 파일을 읽는 방식(현재 `scripts/dashboard/server.js` 패턴)은 SEA exe 안에서는 성립하지 않는다(exe 옆에 파일이 없으므로 404).
- 프런트엔드 정적 파일(html/css/js/svg/png 등)은 전부 `sea-config.json`의 `assets`로 등록하고, 런타임에는 `require('node:sea').getAsset()`으로 서빙한다. MIME 맵을 `.svg/.png/.jpg/.woff2/.ico` 등으로 확장한다.
- `scripts/dashboard`는 SEA 없이 로컬 조회용으로만 쓰는 별도 도구이므로 이 규칙 대상이 아니다(RULES.md §7 참고).

## 4. DB: SQLite (`node:sqlite`)

- 드라이버는 Node 24 내장 `node:sqlite`(`DatabaseSync`)를 쓴다. 네이티브 addon(better-sqlite3)이 필요 없어 SEA 패키징이 단순해진다.
- 기동 시 다음 PRAGMA를 고정한다: `journal_mode=WAL`, `busy_timeout=5000`, `foreign_keys=ON`, `synchronous=NORMAL`. WAL 모드이므로 백업 대상은 `.db` + `.db-wal` + `.db-shm` 세 파일 세트임을 명시한다.
- 마이그레이션은 `PRAGMA user_version` 기반 순차 스크립트로 관리하고, exe 기동 시 자동 적용한다(T003에서 구현).
- `DatabaseSync`는 **동기 API**다. 통합 검색(T051)·주간 집계(T053)처럼 오래 걸리는 쿼리가 이벤트 루프를 막아 다른 사용자의 요청이 밀리지 않도록, 인덱스·페이지네이션을 필수로 하고 필요 시 `worker_threads`로 분리한다.

## 5. 데이터 · 사진 저장 경로

- DB 파일과 사진 폴더는 **동기화 대상이 아닌 로컬 경로**(`%LOCALAPPDATA%\SetupManager\`)에 둔다. OneDrive/네트워크 드라이브 위에 두면 파일 잠금 충돌·손상 위험이 있다(이 저장소 자체가 OneDrive 하위에 있다는 점을 반면교사로 삼는다). WAL의 `-shm` 파일은 네트워크 공유에서 동작하지 않는다.
- 기동 시 데이터 경로가 알려진 동기화 폴더(OneDrive 등) 하위인지 감지해 경고를 남긴다.
- 사진 업로드는 스트리밍 저장, 용량 상한, 파일명은 `UUID + 확장자 화이트리스트`로 생성(원본 파일명은 DB 컬럼에만 보관 — 한글/특수문자·`..\`·예약어(`CON`,`NUL` 등) 경로 문제 회피). 구체 구현은 T049.

## 6. 인증 / 세션

- 세션은 SQLite 테이블 + `httpOnly`, `SameSite=Strict` 쿠키로 관리한다.
- 비밀번호 해시는 **비동기** `crypto.scrypt`를 쓴다(동기 `scryptSync`는 이벤트 루프를 막는다). 파라미터 상향 시 기본 `maxmem`(32MB) 초과로 에러가 나므로 `maxmem`을 함께 지정한다.
- 평문 HTTP(사내망 전제)를 수용한 리스크로 명시한다. 상태 변경 요청(POST/PUT/DELETE)에는 `Origin` 헤더 검증으로 최소 CSRF 방어를 추가한다. 세션 TTL과 만료 세션 정리 주기를 둔다.

## 7. 실시간 갱신

- SPEC §3의 "소규모 동시 접속을 실시간으로 공유"를 만족하기 위해 화면 갱신은 **SSE(`text/event-stream`)** 기반 변경 브로드캐스트를 기본 전략으로 한다(내장 http만으로 구현 가능, 폴링보다 즉시성이 높음).
- `scripts/dashboard`처럼 폴링이 더 적합한 보조 도구에는 4초 이상 주기의 폴링을 허용한다.

## 8. 리포트 — 주간 PPT(T054) 예외

- Node 내장 `zlib`에는 gzip/deflate 스트림만 있고 **ZIP 컨테이너 라이터가 없다**(직접 확인: `Gzip/Gunzip/Unzip/gzip*/unzip*`만 존재). PPTX는 OOXML(ZIP 컨테이너)이므로 "런타임 의존성 0" 원칙만으로는 T054를 만들 수 없다.
- **예외로 결정**: 주간 PPT 생성 기능에 한해 런타임 의존성 1개(zip writer 또는 pptxgenjs 등)를 허용한다. 정형 텍스트 리포트(SPEC §3의 "리포트")는 이 예외와 무관하게 의존성 없이 구현한다. 구체 라이브러리 선택은 T054 착수 시점에 재검토한다.

## 9. 배포 운영

- exe는 코드서명하지 않는다 — 첫 실행 시 Windows SmartScreen 경고가 뜰 수 있음을 배포 안내에 명시한다.
- 네트워크 바인드 시 Windows 방화벽 인바운드 허용 팝업이 뜨므로, IT 담당자의 1회 승인이 필요함을 전제로 문서화한다.
- 서버 PC는 고정 IP 또는 호스트명으로 접속한다고 가정한다(DHCP로 IP가 바뀌면 전 사용자 북마크가 깨짐).
- 중복 실행 방지를 위해 포트 바인드 실패 시 "이미 실행 중" 안내 후 기존 서버로 브라우저만 열어주는 단일 인스턴스 락을 둔다(같은 SQLite 파일을 두 프로세스가 잡는 사고 방지).
- **exe 전달 채널을 메일 첨부/브라우저 다운로드로 하지 않는다.** T007 스파이크에서 실측: Mark-of-the-Web(다운로드 표시)이 붙은 exe는 비대화형 실행 시 Windows가 자동 취소한다(`Start-Process` → "작업이 사용자에 의해 취소되었습니다"). USB 또는 인트라넷 공유 + 필요 시 `Unblock-File`로 배포한다. (PACKAGING.md 참고)

## 10. 확인이 필요한 전제 / T007에서 실제 검증한 결과

T007 스파이크(PACKAGING.md)에서 아래 항목을 실측했다 — 이 문서 초안 때는 미검증이었다.

- ~~사내 PC 브라우저가 최신 Edge/Chrome 계열이라는 전제~~ → **여전히 미검증** (이 세션엔 사내 표준 이미지 PC가 없음)
- ~~Node SEA/`postject`가 사내 환경에서 막히는지~~ → **개발 PC에서는 성공**(esbuild 번들 → SEA → 외부 검증 PASS, WAL 영속성 확인, 콜드스타트 ~310~335ms, exe 88.4MiB). **단, 실제 서버 PC·사내 백신에서는 이 세션 환경 한계로 여전히 미검증** — T002 착수 전 별도로 확인 필요.
- ~~better-sqlite3 대비 `node:sqlite` 실측 비교~~ → **범위에서 제외하기로 결정**(시간 제약). node:sqlite 자체의 기본 동작(WAL, 강제종료 복구, 영속성)은 전부 정상 확인되어 §4의 채택 근거는 유지된다.
- **정정**: postject 주입이 "서명을 손상시켜 남긴다"고 썼으나, 실측 결과 서명 블록 자체가 제거되어 **완전 무서명**이 된다(`signtool remove` 시도 시 이미 서명 없음으로 에러). §9의 "코드서명 안 함" 전제와 결과적으로 같아 별도 조치는 불필요.
- **신규 확인**: `NODE_OPTIONS` 환경변수는 SEA exe에서도 유효하다(V8 힙 한도 변경 실측 확인) — 필요 시 배치파일/시스템 환경변수로 튜닝 가능.

## 리뷰 반영 로그

opus 적대적 리뷰(치명적 5건, 중요 12건, 사소 7건)에서 이 문서에 반영한 항목과 보류한 항목이다.

**반영함** (본문에 결정으로 명시): Node 버전 고정(C1), 런타임/빌드타임 의존성 구분 + 번들러 허용(C2), SEA 자산 서빙 방식(C3), PPT의 zip 의존성 예외(C5), 동기 DB 이벤트 루프 차단 대책(I1), WAL/busy_timeout 등 PRAGMA(I2), 데이터 경로의 동기화 폴더 회피(I3), SSE 기반 실시간 갱신(I4), 서명/방화벽 배포 리스크(I5), 고정 IP 전제(I6), 단일 인스턴스 락(I7), 세션 쿠키·CSRF·비동기 scrypt(I8, S6), better-sqlite3 배제 근거 재검증 필요성 인정(I9 — "확인 필요" 항목으로 하향), lint/build 스크립트 채택(I11), 사진 업로드 원칙(I12), 마이그레이션 전략(S1), 백업 대상 파일 세트(S2), 브라우저 전제 명시(S5), exe 예상 크기 관련 기대치 조정 필요성(S7 — 아래 참고).

**보류함(사용자 확인 필요, 이 작업 범위 밖)**: **C4 — T007(exe 패키징 PoC)이 T002~T006보다 뒤에 있어 가장 위험한 가정이 늦게 검증되는 문제.** 백로그 의존성 순서를 바꾸는 일이라 T001 범위를 넘는다고 판단해 손대지 않았다. 아래 "다음 행동"에서 별도로 보고한다. **I10(SEA 자체의 실험적 지위에 대한 2차 대안 부재)**은 §10에 "확인 필요"로만 남기고 구체 대안 선정은 T007로 미룬다. **S3(날짜/타임존 표현), S4(모듈 캐시 정책)**는 T003/T002 착수 시점에 결정하는 것이 더 적절해 지금 확정하지 않았다.

**exe 크기(S7)**: SEA 산출물은 통상 90~120MB로, SPEC §3의 "경량"이라는 기대와 어긋날 수 있다. T007 스파이크에서 실측치를 내고 이해관계자와 기대치를 맞춘다.
