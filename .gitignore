node_modules/
.claude/settings.local.json

# build output (fast bundle is tiny; the packaged exe is not committed)
dist/
dist/*.exe
*.blob
.eslintcache

# local data / secrets (never committed — see ARCHITECTURE.md §5, §6)
/data/
*.db
*.db-wal
*.db-shm
.env
.env.*
*.pem
*.key
*.log
