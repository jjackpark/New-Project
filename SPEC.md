# Setup Manager (SM) — 제품 스펙

> 표기 규칙: 이 문서와 와이어프레임의 설비명은 실제 기종명이 아니라 "A설비, B설비…" 같은 익명 예시입니다. 실제 데이터에는 실무자가 등록한 설비명이 들어갑니다.

## 1. 한 줄 정의

설비 셋업 관리자·실무자를 위한 사내 웹 도구. 과거 셋업 이슈·이력을 몰라서 동일 문제 재발 시 조치가 늦는 문제를 해결한다. 관리자는 현황을 실시간으로 확인하고, 실무자는 정해진 업무를 진행하며 표준 규격의 리포트를 자동 발행한다. 셋업 이력은 시간이 지날수록 쌓이는 조직 자산이다.

## 2. 사용자

- **관리자**: 전 프로젝트 조회 + 설정 변경 권한 (계정 생성 포함)
- **실무자**: 담당 프로젝트 조회 + 입력 권한. 실질적으로 업체·인력까지 관리하는 관리자급 실무를 수행함 (업체 리스트, 업체 소속 인력, 일보 작성 등)
- 사용 시점: 업체와의 아침 회의, 셋업 마감 미팅 후 퇴근 전 일보 작성
- 동시 접속 규모: 소규모 (관리자 1~2명 + 실무자 수명)

## 3. 기술 요구사항 (제약 조건)

- **배포 형태**: 서버 PC(오피스 내 접속 가능, 별도 실행환경 설치 불가)에 경량 실행파일(exe) 하나를 두고 더블클릭으로 실행. 설치 과정 없음.
- **DB**: 외부 DB 설치 없이 파일 기반이어야 함. 소규모 동시 접속을 실시간으로 공유.
- **인증**: 간단한 사내 계정(자체 관리), 세션 기반. SSO/AD 연동 안 함.
- **사진**: 서버 PC 폴더에 저장, DB엔 경로만 기록.
- **리포트**: 정형 텍스트 자동 생성까지만. 실제 이메일 발송 자동화는 하지 않음(수동 복사/발송).
- **알림**: 없음. 화면 조회로 충분(담당자 지정 알림 기능 불필요).
- **화면 대상**: 데스크톱/모니터 전용. 모바일 대응 불필요.

## 4. 비목표 (하지 않을 일)

- 제작관리, 투자관리
- 실제 이메일 자동 발송, 알림/푸시
- 모바일 반응형
- SSO/AD 연동

## 5. 데이터 모델

### 5.1 핵심 엔티티

| 엔티티 | 주요 필드 | 비고 |
|---|---|---|
| `users` | 아이디, 비밀번호, 이름, 권한(admin/worker) | |
| `projects` | 프로젝트명, 고객사, 상태 | 최상위 스코프. 다른 모든 엔티티는 프로젝트에 종속 |
| `equipment` | 이름(≤12자), 모델, 상태, 담당자, 업체(vendorId) | 프로젝트 소속 설비. 상태값: 가동중/대기/알람/정지 |
| `vendors` (업체) | 업체명, 리더 이름, 비고 | 설비마다 1개 업체 연결(N:1) |
| `personnel` (인력) | 이름, 소속 업체(vendorId), 직무(role), 주 투입 설비(선택) | 업체 소속 인력 |
| `roleOptions` (직무 프리셋) | 직무명 | 설정탭에서 관리. 초기값: PM, 리더, 설계, 전장, 공정, 제어, PLC. 인력 등록 시 자유 입력 + 이 목록을 추천으로 제시 |
| `masterPlanItems` | 항목명, 순서 | 설정탭에서 프로젝트별로 커스터마이징하는 항목 템플릿 (예: 설비 입고 IO체크, 모터체크, 8대 Qual 일정, 사전평가, 시스템평가, MFQ) |
| `masterPlanEntries` | 항목명, 계획일, 실적일, 진척률(%), 상태 | 항목별 실적 |
| `qualItems` | 설비, Qual 항목명, 계획일, 상태 | 8대 Qual 체크리스트 |
| `qualRuns` (런평가) | 설비, 평가종류(사전평가/시스템평가/MFQ), 런 회차, 결과, 일자, 비고 | Qual 완료 후 진행. Qual과 같은 화면의 하위 메뉴로 묶음 (별도 최상위 탭 아님) |
| `issues` | 제목, 관련 설비, 원인, 조치/결과, 심각도, 상태 | 사람이 판단해서 등록하는 문제 |
| `alarms` | 설비, 알람코드, 내용, 상태 | 설비가 자동/수동으로 발생시키는 이벤트. 이슈와 역할 분리 |
| `dailyReports` (일보) | 날짜, 설비, 내용, 작성상태, 업체별 인원수(vendorCounts) | 아래 5.2 참고 |
| `photos` | 설비, 설명, 파일 경로 | |

### 5.2 일보의 업체별 인원수

- 일보 하나에 여러 업체의 투입 인원수를 함께 기록: `(업체, 인원수)` 쌍의 리스트
- **입력 UI 동작**: 일보 작성 화면을 열면 해당 프로젝트에 등록된 업체 목록이 행으로 자동 나열되고(업체를 매번 선택할 필요 없음), 각 행에 인원수 입력칸만 채우면 됨. 그날 투입되지 않은 업체는 0 또는 공란으로 둠. 업체가 새로 추가되면 다음 일보부터 자동으로 행이 늘어남 — 별도로 "행 추가" 버튼을 누를 필요는 없음.
- 일보 상세/화면 하단에 업체별 인원수 합계도 같이 표시 (예: A업체 4명 · B업체 3명 · 합계 7명)
- **주간보고서 집계에는 포함하지 않음** — 일보 화면/상세에서만 노출되는 정보

### 5.3 관계

```
Project 1─N Equipment ──N:1─ Vendor 1─N Personnel
Project 1─N Vendor
Project 1─N RoleOptions
DailyReport(Project, 날짜) 1─N (Vendor, 인원수)
```

설비 하나당 업체는 1개, 업체 하나에는 여러 설비가 걸릴 수 있다. 인력은 업체에 소속되며, 특정 설비에 주로 투입되는 경우 그 설비를 선택적으로 연결한다.

### 5.4 참고: "담당자" 필드 처리

이슈/Qual/마스터플랜/알람의 "담당자" 성격 필드는 인력(personnel) 목록이 생기는 만큼, 자유 텍스트 대신 **인력 목록에서 선택 + 필요시 자유 입력**으로 만드는 것을 권장한다.

### 5.5 상태값(enum) 전체 목록

모든 상태 필드는 아래 값만 허용한다 (자유 텍스트 아님, 드롭다운 선택):

| 필드 | 허용값 |
|---|---|
| `equipment.status` | 가동중 / 대기 / 알람 / 정지 |
| `masterPlanEntries.status` | 예정 / 진행중 / 완료 / 지연 |
| `qualItems.status` | 예정 / 진행중 / 완료 / 미결 |
| `qualRuns.result` | 진행중 / 완료 / 높음 / 보통 / 낮음 |
| `issues.severity` | 낮음 / 보통 / 높음 |
| `issues.status` | 미해결 / 조치중 / 완료 |
| `alarms.status` | 발생 / 조치중 / 해제 |
| `dailyReports.status` | 작성중 / 작성완료 |
| `projects.status` | 진행중 / 완료 / 보류 |
| `users.role` | admin / worker |

## 6. 화면(탭) 명세

| # | 탭 | 목적 |
|---|---|---|
| 1 | 메인 화면 | 프로젝트 KPI (전체 설비, 미해결 이슈, 진행중 알람, 금일 일보 등) |
| 2 | 프로젝트 | 프로젝트 등록/전환 (전역 스코프 전환) |
| 3 | 설비 현황 | 설비 목록/등록/수정. 업체 연결 포함 |
| 4 | 업체/인력 | 업체 등록, 업체별 인력 등록. 일보 작성에서 자주 참조되므로 접근성이 좋아야 함 (최상위 탭 권장) |
| 5 | 마스터 플랜 | 설정탭에서 정의한 항목별 계획/실적 |
| 6 | Qual / 런평가 | 8대 Qual 체크리스트 + 하위 메뉴로 런평가(사전평가/시스템평가/MFQ) |
| 7 | 이슈리스트 | 사람이 등록하는 문제. 알람과 역할 분리 |
| 8 | 알람 관리 | 설비 발생 이벤트 |
| 9 | 일보 | 당일 진행 내용 + 업체별 투입 인원수 |
| 10 | 캘린더 | 이슈/Qual/마스터플랜 일정 통합 뷰 |
| 11 | 사진관리 | 업로드/목록, 서버 폴더 저장 |
| 12 | 검색 | 프로젝트/기간을 가로지르는 통합 이력 검색 |
| 13 | 설정 | 마스터플랜 항목 관리, 직무 프리셋 관리, 사용자 계정 관리(관리자 전용) |

리포트 자동 생성(일일 정형 텍스트, 주간 PPT)은 화면 탭이 아니라 각 데이터를 모아 생성하는 별도 기능이다.

### 6.1 메인 화면 KPI 정의

| KPI | 정의 |
|---|---|
| 전체 설비 | 현재 프로젝트에 속한 `equipment` 총 개수 |
| 미해결 이슈 | 현재 프로젝트의 `issues` 중 `status ≠ 완료`인 개수 |
| 진행중 알람 | 현재 프로젝트의 `alarms` 중 `status ≠ 해제`인 개수 |
| 금일 작성 일보 | 오늘 날짜(`date = 오늘`)로 등록된 `dailyReports` 개수, 가능하면 "작성완료/전체설비" 형태(예: 4/5건)로 표시해 미작성 설비를 바로 알 수 있게 함 |
| 설비 상태 분포 | `equipment.status`별 개수 (가동중/대기/알람/정지) — 카드가 아니라 목록 또는 도넛 차트 형태 |
| 최근 등록 사진 | `photos`를 등록일 최신순으로 3~4장, 썸네일+설비명+등록자+시각으로 미리보기. 클릭 시 사진관리 탭으로 이동 |

### 6.2 화면 예시 (와이어프레임)

`docs/dashboard-mockup.svg` — 메인 화면 레이아웃 참고용. 실제 수치·텍스트·설비명은 예시이며, 카드 배치와 정보 위계(제목 → KPI 4개 → 설비 상태 카드 → 업체별 인원수/최근 이슈·알람 2단 구성 → 최근 등록 사진)만 참고한다. 디자인을 그대로 복제할 필요는 없고, 정보 구조와 톤(좌측 다크 사이드바, 화이트 카드, 색상 뱃지)만 지키면 된다.

## 7. UI/UX 원칙

- **대상 해상도**: 데스크톱/모니터 전용, 최소 1024px 폭 기준
- **텍스트 짤림 방지 3단계** (반드시 이 순서로 적용):
  1. 입력 단계 글자수 제한 (예: 설비명 12자, 담당자/인력명 6자) — 예방
  2. 자동 축소 — 폰트 크기를 일정 범위 내에서만 축소 (무한정 줄이지 않음)
  3. 최후 수단 — 말줄임(`...`) + 마우스 오버 시 전체 텍스트 툴팁
- **디자인 톤**: 좌측 다크 네이비 사이드바, 상단 KPI 카드, 화이트 카드 기반 레이아웃, 상태는 색상 뱃지로 표시 (가동중=초록, 대기=회색, 알람/정지=빨강 등)

## 8. 구현 우선순위

1. 기반 데이터: 프로젝트, 설비, 업체/인력, 직무 프리셋, 설정탭
2. 핵심 입력: 마스터플랜, Qual(+런평가), 이슈리스트, 알람 관리, 일보(업체별 인원수 포함)
3. 시각화: 메인 화면 KPI, 일일 리포트 자동 생성
4. 확장: 캘린더, 사진관리, 검색
5. 고급: 주간보고서(PPT) 자동 생성 — 업체별 인원수는 집계에서 제외
