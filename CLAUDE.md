# CLAUDE.md

이 저장소에서 작업하는 Claude Code를 위한 프로젝트 가이드다. 구체적인 강제 규칙은 [RULES.md](RULES.md)에 있으니 작업 전에 함께 읽는다.

## 프로젝트 한눈에 보기

**Setup Manager (SM)** — 설비 셋업 관리자·실무자를 위한 사내 웹 도구. 과거 셋업 이슈·이력을 몰라 동일 문제가 재발하는 것을 막는 것이 목적이다. 관리자는 현황을 실시간으로 보고, 실무자는 정해진 업무(마스터플랜/Qual/이슈/알람/일보)를 진행하며 표준 리포트를 자동 발행한다.

전체 스펙은 **[SPEC.md](SPEC.md)**가 원본이다. 요약이 SPEC.md와 어긋나면 SPEC.md가 우선한다.

### 현재 상태

- 아직 기술 스택 미결정 (T001이 `todo`, 시작 가능한 유일한 작업). 코드는 아직 없고 스펙/백로그/훅 도구만 갖춰진 계획 단계다.
- 작업은 `backlog.json` (57개 태스크, `tasks/T0NN.md` 문서와 1:1 대응)로 관리한다. 조회/수정은 반드시 `node scripts/backlog-cli.js` 사용 — 자세한 건 RULES.md.
- 구현 우선순위(SPEC.md §8): ① 기반 데이터(프로젝트/설비/업체·인력/직무프리셋/설정) → ② 핵심 입력(마스터플랜/Qual+런평가/이슈/알람/일보) → ③ 시각화(메인 KPI/일일 리포트) → ④ 확장(캘린더/사진관리/검색) → ⑤ 고급(주간 PPT 리포트).

## 제약 조건 (반드시 지킬 것)

- **배포**: 오피스 서버 PC에 설치 없이 더블클릭 실행하는 경량 exe 하나. 별도 실행환경 설치 불가.
- **DB**: 외부 DB 서버 없이 파일 기반. 소규모 동시 접속(관리자 1~2명 + 실무자 수명)을 실시간 공유해야 한다.
- **인증**: 자체 관리하는 간단한 세션 기반 계정. SSO/AD 연동 없음.
- **사진**: 서버 PC 폴더에 저장, DB에는 경로만 기록.
- **리포트**: 정형 텍스트 자동 생성까지만. 실제 이메일 발송 자동화는 하지 않는다(수동 복사/발송 전제).
- **알림**: 없음. 화면 조회로 충분하다고 전제한다.
- **화면**: 데스크톱/모니터 전용, 최소 1024px. 모바일 대응 불필요.

**비목표**: 제작관리, 투자관리, 실제 이메일 자동 발송/푸시 알림, 모바일 반응형, SSO/AD 연동. 이 범위를 넘는 기능을 임의로 추가하지 않는다.

## 데이터 모델 요약

`projects`가 최상위 스코프이며 나머지 엔티티는 전부 프로젝트에 종속된다: `equipment`(N:1 `vendors`), `vendors` 1─N `personnel`, `roleOptions`, `masterPlanItems`/`masterPlanEntries`, `qualItems`/`qualRuns`(런평가는 Qual의 하위 메뉴, 별도 최상위 탭 아님), `issues`(사람이 등록)와 `alarms`(설비가 발생, 이슈와 역할 분리), `dailyReports`(업체별 인원수 리스트 포함, 주간보고서 집계에는 미포함), `photos`, `users`.

모든 상태(enum) 필드는 자유 텍스트가 아니라 SPEC.md §5.5에 정의된 값만 허용한다(예: `equipment.status`는 가동중/대기/알람/정지). 새 상태값이 필요하면 SPEC.md를 먼저 갱신하고 구현한다.

담당자류 필드(이슈/Qual/마스터플랜/알람)는 자유 텍스트 대신 `personnel` 목록에서 선택 + 자유 입력 보완 방식을 우선한다(SPEC.md §5.4).

## 화면 / UI·UX 톤

13개 탭 구성은 SPEC.md §6 표를 따른다. 메인 화면 와이어프레임은 **`dashboard-mockup.svg`**(저장소 루트; SPEC.md 본문의 `docs/dashboard-mockup.svg` 경로 표기는 실제 위치와 다르므로 루트 파일을 참조)를 톤·정보 위계 참고용으로 쓴다 — 디자인을 그대로 복제하지 말고 다음만 지킨다:

- 좌측 다크 네이비 사이드바 + 화이트 카드 기반 레이아웃, 상단 KPI 카드 4개
- 정보 위계: 제목 → KPI 4개 → 설비 상태 카드 → (업체별 인원수 / 최근 이슈·알람) 2단 → 최근 등록 사진
- 상태는 색상 뱃지로: 가동중=초록, 대기=회색, 알람/정지=빨강 계열
- 텍스트 짤림 방지는 **반드시 이 순서**로: ① 입력 글자수 제한(설비명 12자, 담당자/인력명 6자 등) → ② 폰트 자동 축소(범위 내에서만) → ③ 말줄임(`...`) + 툴팁. 순서를 건너뛰지 않는다.

메인 화면 KPI 정의(전체 설비/미해결 이슈/진행중 알람/금일 작성 일보/설비 상태 분포/최근 등록 사진)는 SPEC.md §6.1을 그대로 따른다.

## 전용 Subagent

이 저장소는 `.claude/agents/`에 두 개의 전용 subagent를 정의해 둔다. 사용 시점과 강제 여부는 RULES.md §7을 따른다.

- **`backlog-briefer`** (haiku) — 백로그 태스크 ID를 받아 쉬운 설명 + 관련 파일 목록을 조사해 `tasks/<ID>.md`의 `## 작업 노트`에 정리해 넣는다. 태스크를 `in_progress`로 돌리기 전에 호출한다.
- **`adversarial-reviewer`** (opus) — 이번 작업에서 생성/변경한 파일에 대해 일부러 적대적인(트집 잡는) 피드백만 준다. 파일을 고치지는 않는다. 파일을 만들거나 고친 직후, 특히 `review`/`done`으로 넘기기 전에 호출한다.

## 이 저장소에서 작업할 때

- `backlog.json`을 직접 Read/Edit/Grep/cat 하지 않는다 — 훅이 차단하고 CLI 사용법을 안내한다. `node scripts/backlog-cli.js help` 참고.
- 태스크 상태를 `done`으로 바꾸면 자동으로 커밋 + push까지 실행된다(훅 동작). 검증되지 않은 작업을 `done`으로 넘기지 않는다. 자세한 훅 동작은 RULES.md 참고.
- `main`이 아니라 `dev` 브랜치에서 작업한다.
- 코드 파일은 300줄을 넘기지 않는다(초과 시 Write/Edit이 차단됨). 처음부터 책임을 작게 쪼갠다.
- 기술 스택이 정해지고 `package.json`에 `lint`/`build` 스크립트가 생기면, 이후 모든 Write/Edit와 세션 종료 시 자동으로 lint/build가 돌고 실패하면 계속 작업하도록 막힌다 — 별도 설정 없이 이미 훅이 준비되어 있다.
